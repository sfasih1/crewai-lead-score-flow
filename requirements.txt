# Runtime dependencies for crewai-lead-score-flow
# Derived from pyproject.toml with small adjustments:
# - Dropped the asyncio backport (stdlib in Python 3.10+)
# - Added python-dotenv and pydantic explicitly for clarity

crewai[tools]==0.85.0
# crewai[tools] already brings crewai-tools, but we pin it explicitly to match lock
crewai-tools==0.17.0
langchain-tools>=0.1.34
google-auth-oauthlib>=1.2.1
google-api-python-client>=2.145.0
pyvis>=0.3.2
python-dotenv>=1.0.1
pydantic>=2.7.0

# Copy this file to `.env` and fill in your secrets.
# Never commit your real `.env` to git.

# --- Provider keys (choose ONE provider to start) ---
# OpenRouter (recommended for easy model variety)
OPENROUTER_API_KEY="your-openrouter-key"
# Optional: helps OpenRouter attribute requests
# OPENROUTER_REFERRER="https://yourdomain.com"

# OpenAI (if using OpenAI directly)
# OPENAI_API_KEY="sk-..."

# Perplexity (if using Perplexity models)
# PERPLEXITY_API_KEY="pplx-..."

# Azure OpenAI (if using Azure)
# AZURE_OPENAI_API_KEY="..."
# AZURE_OPENAI_ENDPOINT="https://<your-endpoint>.openai.azure.com/"
# AZURE_OPENAI_API_VERSION="2024-07-18-preview"

# --- Model selection ---
# If not set, the app will pick a reasonable default for your provider and
# try fallbacks automatically if a route isn’t available.
# Recommended stable choices:
#   - openrouter/openai/gpt-4o-mini
#   - openrouter/deepseek/deepseek-chat
#   - openrouter/meta-llama/llama-3.1-8b-instruct
LLM_MODEL="openrouter/openai/gpt-4o-mini"

# --- Runtime toggles ---
# Enable the deep-dive LLM step (set to 0 to disable)
USE_LLM_DEEPDIVE=1

# Prints extra provider/model diagnostics (optional)
# LITELLM_DEBUG=1
# API_SANITY_CHECK=1

# --- Input convenience (optional) ---
# Provide a full path to your contacts CSV to skip the file picker
# LEADS_CSV="C:\\Users\\you\\Desktop\\Contacts.csv"
